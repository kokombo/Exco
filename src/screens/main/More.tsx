import { StyleSheet, Text, SafeAreaView } from "react-native";

const More = () => {
  return (
    <SafeAreaView>
      <Text>More</Text>
    </SafeAreaView>
  );
};

export default More;

const styles = StyleSheet.create({});
